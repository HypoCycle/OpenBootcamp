#Escribe una función que calcule el área de un triángulo, recibiendo
#la altura y la base como parámetros y otra función que calcule el área 
#de un círculo recibiendo el radio del mismo.


import math


def areatriangulo(altura, base):
    return base * altura / 2

print(areatriangulo(10, 15))


def areaCirculo(radio):
    return math.pi * radio**2

print("{:.2f}".format(areaCirculo(10))
